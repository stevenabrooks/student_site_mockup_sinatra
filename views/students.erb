
<h1>This is supposed to be a list of students</h1> 
  <ul>
    <% @students.each do |student| %>
      <li>
        <a href=<%="students/#{student.id}"%>><%= student.name %></a>
        <a href=<%="https://google.com"%>>Google</a>
      </li>
    <% end %>
  </ul>
